import { SkillsDirective } from './skills.directive';

describe('SkillsDirective', () => {
  it('should create an instance', () => {
    const directive = new SkillsDirective();
    expect(directive).toBeTruthy();
  });
});
